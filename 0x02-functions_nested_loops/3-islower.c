#include"main.h"

/**
 * main - check the code.
 *
 * Return: 1 for small and 0 for not small 
 */

int _islower(int c)
{

   if (( 67 <= c ) && ( c <= 91 ))

   return (1);

   else 

   return (0);

}
